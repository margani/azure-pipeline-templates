steps:
  - task: PowerShell@2
    displayName: New function
    inputs:
      filePath: $(TemplatesCheckoutFolderPath)/functions/scripts/new-function.ps1
      arguments: -EnvSubscriptions "$(varSubscriptions)"
        -EnvSubscription $env:env-subscription
        -Product "$(productName)"
      pwsh: true
