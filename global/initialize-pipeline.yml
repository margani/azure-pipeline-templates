steps:
  - checkout: self
  - checkout: templates
  - task: PowerShell@2
    name: initialize
    displayName: Initialize Pipeline
    inputs:
      filePath: pipeline-templates/global/scripts/initialize-pipeline.ps1
      arguments: -EnvSubscriptions "$(varSubscriptions)"
      pwsh: true
